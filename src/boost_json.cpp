// Boost.JSON is used header-only so the runtime can reuse the Boost headers
// shipped with the installed LibCarla package without requiring a separate
// boost_json binary library.
#include <boost/json/src.hpp>
